const empObj = {
    name : "Sourabh",
    salary : 4000000,
    profile : "SmartContractDeveloper"
}

function numberOfKeys(empObj){
   console.log(Object.keys(empObj).length);
}
numberOfKeys(empObj);